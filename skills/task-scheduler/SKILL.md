---
name: task-scheduler
description: Schedule prompts/tasks to run automatically on an interval.
---
# Task Scheduler
Turn a recurring request into a scheduled task (name, target agent, prompt, interval) using the
hub's Automations (`POST /api/schedules`). Confirm the schedule and how to pause/delete it.
